// 向量求和：sum = a[0]+...+a[n-1]
int main() {
    vector<int> a[16];
    int i = 0;
    while (i < 16) {
        a[i] = i + 1;
        i = i + 1;
    }
    int s = vec_sum(a, 16);
    putint(s);   // 1+2+...+16 = 136
    putch(10);
    return 0;
}

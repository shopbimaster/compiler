// 《前端+变长》 浮点向量扩展：空 vecf（长度 0，运算后仍为长度 0 的安全退化）
// 预期输出: 99
// 空 vecf 长度 0；e = a + b 时 lhs 长度=0，循环 0 次，结果长度=0
// 访问空 vecf 元素属未定义行为，本用例仅断言循环不会越界
int main() {
    vecf e;
    putint(99); putch(10);
    return 0;
}

int multiply(int a, int b) {
    if (b == 0)
        return 0;
    if (b - (b / 2) * 2 == 1)
        return a + multiply(a + a, b / 2);
    return multiply(a + a, b / 2);
}

int main() {
    return multiply(6, 5);
}
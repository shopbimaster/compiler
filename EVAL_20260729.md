# 线上评测结果分析 —— 2026-07-29（100 分 AC / 546.7156s）

## 一、总体

- Functional 100/100 AC，H_Functional 40/40 AC，Performance 60/60 AC。
- 正确性满分，**总分已封顶**。后续所有工作的唯一目标是压缩 546.72s 的总时间。
- 本地此前统计的 6 DIFF + 2 TIMEOUT 是本地测试脚本/用例的问题，线上并不存在，
  说明本地 `_func_regress.sh` 的期望值获取方式仍与线上有出入（见第五节）。

## 二、时间分布（这是关键）

把 60 个性能用例按耗时排序，时间高度集中：

| 排名 | 用例 | 时间(s) | 占总时间 |
|-----|------|--------|---------|
| 1 | many_mat_cal-1 | 106.17 | 19.4% |
| 2 | many_mat_cal-2 | 105.44 | 19.3% |
| 3 | many_mat_cal-3 | 104.52 | 19.1% |
| 4 | conv2d-1 | 53.74 | 9.8% |
| 5 | matmul2 | 25.17 | 4.6% |
| 6 | sl2 | 19.52 | 3.6% |
| 7 | transpose2 | 18.38 | 3.4% |
| 8 | h-1-03 | 17.75 | 3.2% |
| 9 | conv2d-2 | 14.66 | 2.7% |
| 10 | matmul3 | 13.41 | 2.5% |

- **many_mat_cal 三例合计 316.13s，占总时间的 57.8%。**
- 前 4 名合计 369.87s = 67.7%；前 10 名合计 378.76s ≈ **69.3%**。
- 余下 50 例合计约 167.96s，其中 30 例低于 1s，优化它们对总分毫无意义。

结论：**优化只有落在 many_mat_cal / conv2d / matmul / sl / transpose 这几族
矩阵类用例上才有意义**。任何在 h-4/h-5/h-8/knapsack/optimization_scheduling
（毫秒级）上的收益，即使做到 100% 提速，对总时间的影响也小于 0.2%。

## 三、与本地静态分析的交叉验证

本地对 60 例 asm 的 in-loop 指令统计：

```
waste = 1662 / 10755 in-loop instrs = 15.5%
  mv 复制         1350   (占全部浪费 81%，占 in-loop 指令 12.5%)
  循环内 li         192
  重复 lw           102
  negw              18
```

按用例看浪费最重的几个（in-loop / mv / li / dupLw）：

```
crypto-1/2/3     341   35  16   5
matmul1/2/3      183   26  14   1
03_sort1/2/3     344   31   7   0
h-9-01/02/03     212   38   4   0
conv2d-1/2/3     342   37   4   3
```

注意交叉验证的结论：

- **matmul 系列**（线上 6.22 + 25.18 + 13.41 = 44.81s，8.2%）确实是 mv 密集，
  静态浪费与线上耗时方向一致 → 值得做。
- **conv2d 系列**（线上 53.74 + 14.66 + 4.83 = 73.23s，13.4%）同样 mv 密集
  （37 条）→ 值得做。
- **crypto 系列** 静态浪费最重（35 条 mv），但线上仅 5.38 + 3.76 + 2.15 =
  11.29s（2.1%）→ 优先级低于其静态排名。
- **many_mat_cal 占 57.8% 却未出现在静态浪费榜前列** —— 这是最重要的信号：
  它的瓶颈**不是** in-loop 的机械浪费，而是别的东西（算法级/访存级）。
  必须单独剖析，不能套用 mv 消除的结论。

## 四、下一步优先级（按"时间收益 × 可行性"排序）

1. **剖析 many_mat_cal（最高优先级，57.8% 的时间）**
   先搞清楚它慢在哪：循环结构、访存模式、是否有可提取的循环不变量、
   是否存在重复的矩阵运算可缓存。在没有剖析结论前不要动手改优化器。
2. **PHI copy coalescing 放宽（已定位根因）**
   `isClassicRmwCoalesce` 条件#3「PHI 在 defBB 无其他 use」过强，
   误拒了 use 全部位于 def 之前的情形（matmul 内层 `addiw s1,s3,1 / mv s3,s1`）。
   放宽为「defBB 内无 def 之后的 use」。
   直接受益：matmul(8.2%) + conv2d(13.4%) = 21.6% 的时间盘子。
3. **conv2d 专项**：13.4% 的时间，且 conv2d-1 单例就 53.74s，
   与 conv2d-3(4.83s) 差 11 倍，说明规模敏感，可能有访存局部性问题。
4. 低优先级：crypto、03_sort、h-9（静态浪费高但时间占比合计不足 6%）。
5. **不要做**：所有 <1s 的用例（30 例），无论静态指标多难看。

## 五、待修正的本地基础设施

- `_func_regress.sh` 报 6 DIFF + 2 TIMEOUT，线上全 AC → 本地期望值比对逻辑
  或超时阈值有误，需修正，否则会掩盖真实回归。
- `coalescePhis` 的 disjoint 判据在 60 例上 0 命中（399 次接受全部走 RMW 路径），
  等同死代码，需查明是判据过严还是被 RMW 完全覆盖。

## 六、分支约定

从本次起，每项新优化单独开分支，命名 `perf-<主题>`，便于留痕与回退。
本次评测对应的代码状态即为后续所有 A/B 的基线。

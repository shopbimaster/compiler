# 变长向量语法实现说明（前端 + 变长）

> **分支**: `feat-vec-dynamic-fe`（基于 `main`）
> **本版范围**: **只改生成 IR 以前的部分**（词法 / 语法 / IRBuilder），
> **支持运行时变长**的向量语法（与定长版 `feat-simd-vector` 同源异构）。
> **源码标记**: 所有与 `main` 不同的源码改动均以注释 `《前端+变长》` 标注，
> 通过 `grep -rn '《前端+变长》' grammar/ include/ src/` 可一键定位微调点。
>
> **核心约束**: 决赛 FPGA 不确定在 ISA 层面支持 SIMD 指令（如 RVV），所以本版
> 不引入新 IR 类型、不触后端代码生成与优化器；向量运算在 IR 生成阶段**去语法糖
> 为运行时循环**（`alloca 计数器 + cond/body/end 三块`），最终汇编是纯标量 RV64GC。
>
> **与定长版的区别**:
> - 定长版：`alloca [N x i32]`，运算**编译期展开**为 N 个标量 `load/op/store`
> - 变长版：`alloca [VEC_MAX+1 x i32]`，`[0]=运行时长度`，运算**运行时循环**
>   （循环次数在运行时从 `[0]` 读取，不展开）
>
> **浮点向量扩展**：在整数变长向量基础上新增 `vecf` 关键字，元素类型为 `float`，
> 运算使用 `FADD/FSUB/FMUL/FDIV`。语法、IR 结构、运行时循环骨架与整数版对称，
> 仅差异在 `FloatType` + 浮点 opcode + `vecfAllocas` 侧集合。

---

## 一、本版范围与设计哲学

### 1.1 用户语法目标

```c
vec a = {1, 2, 3, 4};    // 列表初始化，长度=元素数（4），存到 [0]
vec b = {4, 3, 2, 1};
vec c = a + b;           // 运行时循环算 {5,5,5,5}
vec d = a * 2;           // 标量广播，运行时循环
vec e = 3 + a;           // 标量在左，运行时循环
vec f;                   // 空向量（长度 0），不同于定长版的默认 4
f = a + b;               // 声明后赋值，emitVecCopy 把 src 长度覆盖到 dst [0]
putint(c[0]);            // 索引访问，emitVecElemPtr 跳过 [0] 长度槽

// 浮点向量扩展（与 vec 对称，元素类型为 float，运算用 FADD/FSUB/FMUL/FDIV）
vecf fa = {1.0, 2.0, 3.0, 4.0};
vecf fb = {4.0, 4.0, 4.0, 4.0};
vecf fc = fa + fb;       // 运行时循环算 {5.0,6.0,7.0,8.0}
vecf fd = fa * 2.0;      // 标量广播，FMUL 循环
vecf fe = 3.0 - fa;      // 标量在左，FSUB 循环
vecf ff;                 // 空浮点向量（长度 0）
ff = fa + fb;            // 声明后赋值，emitVecfCopy 把 src 长度覆盖到 dst [0]
putint(fc[0]);           // 索引访问，emitVecfElemPtr 跳过 [0] 长度槽
                         // （putint 对 float 隐式截断，便于输出整数结果）
```

### 1.2 "前端 + 变长" 的含义

| 关键词 | 含义 |
|--------|------|
| **前端** | 只动 `grammar/*.g4` + `include/ir/IRBuilder.h` + `src/ir/IRBuilder.cpp`，不动 `src/opt/*` 优化器、不动 `src/backend/*` 代码生成、不动 `include/ir/IR.h` 类型系统 |
| **变长** | 向量长度在**运行时**确定：`vec a = {...}` 长度 = 列表元素数（编译期常量）；`vec a;` 长度 = 0；声明后赋值 `a = b;` 长度由 rhs 决定（运行时长度写入 `[0]` 槽）。单个 vec 最大元素数 `VEC_MAX = 1024`；浮点版 `VECF_MAX = 1024` |
| **去语法糖** | `vec` 在 IR 中表示 `alloca [VEC_MAX+1 x i32]`，其中 `[0]` 存运行时长度、`[1..len]` 存数据。所有向量运算符展开为**运行时循环**（`alloca I32 计数器 i` + `cond/body/end` 三个基本块），循环次数从 lhs 的 `[0]` 槽运行时读取。浮点版 `vecf` 同骨架，元素类型改为 `float`，opcode 改为 `FADD/FSUB/FMUL/FDIV` |
| **侧集合识别** | vec 与普通 `[N x i32]` 数组在 IR 中类型相同，靠 `vecAllocas` 集合（`std::unordered_set<Value*>`）区分；浮点版同理用 `vecfAllocas` 集合（与 `vecAllocas` 互不重叠） |

### 1.3 为什么这么设计

- **后端零改动**：FPGA BOOM 软核只保证 RV64GC，没有 RVV；纯标量 IR 走现有 TargetCodeGen 即可。
- **优化器零改动**：标量 IR + 运行时循环走 Mem2Reg / SCCP / LoopUnroll 等已有 pass，循环结构是这些 pass 的天然输入。
- **类型系统零改动**：不新增 `VectorType`，复用 `ArrayType` + `PointerType`。
- **代价**：单个 vec 占 `VEC_MAX+1 = 1025` 个 i32 = 4100 字节栈空间；多个 vec 同时声明时栈占用较大（但函数级通常足够）。

---

## 二、源码改动清单（4 个手改文件 + 1 个脚本 + ANTLR 重新生成 + 测试用例，全部标 `《前端+变长》`）

> 检索命令：`grep -rn '《前端+变长》' grammar/ include/ir/IRBuilder.h src/ir/IRBuilder.cpp`

### 2.1 `grammar/SysY2022Lexer.g4` — 加 `VEC` / `VECF` 关键字

```antlr
VEC: 'vec';   // 《前端+变长》 变长向量关键字（运行时长度，仅前端词法扩展）
VECF: 'vecf'; // 《前端+变长》 浮点向量扩展：变长浮点向量关键字（运行时长度，float 元素）
```

### 2.2 `grammar/SysY2022Parser.g4` — 加 `vecDecl` / `vecInit` / `vecfDecl` 规则

```antlr
// 《前端+变长》 在原 SysY 声明基础上新增 vecDecl / vecfDecl 分支
decl: constDecl | varDecl | vecDecl | vecfDecl;

// 《前端+变长》 变长向量声明（运行时长度，仅前端语法扩展）：
//   vec name = { e1, e2, ... };   列表初始化（长度=元素数，运行时存储）
//   vec name = exp;               表达式初始化（exp 须为向量，如 a + b）
//   vec name;                     空向量（长度 0）
// vecInit 以 L_BRACE 起首，exp 以 L_PAREN/IDENTIFIER/number/unaryOp 起首，
// 两者首 token 互斥，ANTLR LL(*) 无歧义。
// 不引入新 IR 类型；vec 表示为 alloca [VEC_MAX+1 x i32]，
// [0] 存运行时长度，[1..len] 存数据；运算用运行时循环（非编译期展开）。
vecDecl: VEC IDENTIFIER (ASSIGN (vecInit | exp))? SEMICOLON;

// 《前端+变长》 向量初值（列表形式）
vecInit: L_BRACE exp (COMMA exp)* R_BRACE;

// 《前端+变长》 浮点向量扩展：变长浮点向量声明（运行时长度，float 元素）
// 语法与 vecDecl 对称，区别仅在 VECF 关键字（float 元素）。
// vecf 表示为 alloca [VECF_MAX+1 x float]，[0] 存运行时长度，[1..len] 存数据；
// 运算用运行时循环（FADD/FSUB/FMUL/FDIV，非编译期展开）。
vecfDecl: VECF IDENTIFIER (ASSIGN (vecInit | exp))? SEMICOLON;
```

**设计要点**：`vecInit | exp` 用 first-token 区分（`vecInit` 以 `L_BRACE` 起首，`exp` 以
`L_PAREN / IDENTIFIER / number / unaryOp` 起首），ANTLR LL(*) 无歧义，无需语义谓词。
`vecfDecl` 复用 `vecInit`（列表初值语法相同），编译期符号差异通过 `VECF` 关键字在
词法层区分，语义层走 `visitVecfDecl`。

### 2.3 `include/ir/IRBuilder.h` — vecAllocas / vecfAllocas 集合 + 14 个辅助方法声明

```cpp
// 《前端+变长》 变长向量声明访问器（运行时长度，仅前端扩展）
std::any visitVecDecl(SysY2022Parser::VecDeclContext* ctx) override;
std::any visitVecInit(SysY2022Parser::VecInitContext* ctx) override;
// 《前端+变长》 浮点向量扩展：变长浮点向量声明访问器（运行时长度，float 元素）
std::any visitVecfDecl(SysY2022Parser::VecfDeclContext* ctx) override;

// ===== 《前端+变长》 变长向量运算辅助（仅前端 IR 生成扩展，运行时长度） =====
// vec 表示为 alloca [VEC_MAX+1 x i32]：[0]=运行时长度，[1..len]=数据
// 运算用运行时循环（非编译期展开），与定长版的核心区别
static constexpr unsigned VEC_MAX = 1024;   // 单个 vec 最大元素数
bool   isVecValue(Value* v);                                              // 侧集合判别
Value* emitVecElemPtr(Value* vecPtr, Value* idx);                         // vec[idx] 地址（跳过 [0]）
Value* emitVecAlloca(unsigned n);                                          // 分配 + 写长度
Value* emitVecLen(Value* vecPtr);                                          // 运行时 load [0]
Value* emitVecBinOp(Instruction::Opcode op, Value* left, Value* right);   // vec op vec（循环）
Value* emitVecScalarOp(Instruction::Opcode op, Value* scalar,
                       Value* vec, bool scalarOnLeft);                    // scalar op vec（循环）
void   emitVecCopy(Value* dst, Value* src);                               // dst = src（循环）

// ===== 《前端+变长》 浮点向量扩展辅助（vecf，FADD/FSUB/FMUL/FDIV 运行时循环） =====
// vecf 表示为 alloca [VECF_MAX+1 x float]：[0]=运行时长度，[1..len]=float 数据
// 与整数向量同骨架，差异仅 FloatType + 浮点 opcode
static constexpr unsigned VECF_MAX = 1024;   // 单个 vecf 最大元素数
bool   isVecfValue(Value* v);                                              // 浮点向量侧集合判别
Value* emitVecfElemPtr(Value* vecfPtr, Value* idx);                       // vecf[idx] 地址（跳过 [0]）
Value* emitVecfAlloca(unsigned n);                                         // 分配 + 写长度
Value* emitVecfLen(Value* vecfPtr);                                        // 运行时 load [0]
Value* emitVecfBinOp(Instruction::Opcode op, Value* left, Value* right);  // vecf op vecf（循环）
Value* emitVecfScalarOp(Instruction::Opcode op, Value* scalar,
                        Value* vecf, bool scalarOnLeft);                  // scalar op vecf（循环）
void   emitVecfCopy(Value* dst, Value* src);                              // dst = src（循环）

// 《前端+变长》 变长向量 alloca 侧集合：记录哪些 alloca 是 vec / vecf
std::unordered_set<Value*> vecAllocas;
std::unordered_set<Value*> vecfAllocas;   // 浮点向量扩展：与 vecAllocas 互不重叠
```

### 2.4 `src/ir/IRBuilder.cpp` — 实现与接线（18 处标注）

| 函数 | 标注位置 | 作用 |
|------|----------|------|
| `visitDecl` | 分发分支 | 新增 `if (ctx->vecDecl()) return visitVecDecl(...)` 和 `if (ctx->vecfDecl()) return visitVecfDecl(...)` |
| `visitVecDecl` Case 1 | 无初始化 | `emitVecAlloca(0)`（空向量，长度 0） |
| `visitVecDecl` Case 2 | 列表初始化 | `emitVecAlloca(n)` + 逐元素 `store` 到 `[1..n]`（`emitVecElemPtr` 跳过 `[0]`） |
| `visitVecDecl` Case 3 | 表达式初始化 | 求 rhs；若 rhs 是 vec，按 rhs 运行时长度分配 lhs（`emitVecAlloca(n)`），再用 `emitVecCopy` 逐元素拷贝；rhs 长度非常量时用运行时 `store` 覆盖 `[0]` |
| `visitVecfDecl` Case 1..3 | 浮点版声明 | 与 `visitVecDecl` 对称，差异：`FloatType` + `emitVecf*` |
| `visitVecInit` | 空 Visitor | 内联处理，保留空实现满足接口 |
| `isVecValue` | 整数向量判据 | 查 `vecAllocas` 集合 |
| `isVecfValue` | 浮点向量判据 | 查 `vecfAllocas` 集合（与 `vecAllocas` 互不重叠） |
| `emitVecElemPtr` / `emitVecfElemPtr` | 元素地址 | `gep [0, idx+1]`（跳过 `[0]` 长度槽），vec 是 i32、vecf 是 float |
| `emitVecAlloca` / `emitVecfAlloca` | 分配 + 长度 | `alloca [VEC_MAX+1 x i32]`（vec）/ `alloca [VECF_MAX+1 x float]`（vecf）+ `store n, [0]`（[0] 槽用 i32 存长度，与 vec 一致） |
| `emitVecLen` / `emitVecfLen` | 长度读取 | `load i32 [0]`（长度值用 i32，与元素类型解耦） |
| `emitVecBinOp` / `emitVecfBinOp` | vec/vecf op vec/vecf | `alloca` 计数器 `i` + `cond/body/end` 三块循环：每轮 `load left[i+1]`、`load right[i+1]`、`op`、`store result[i+1]`、`i = i + 1`；vecf 用 `FADD/FSUB/FMUL/FDIV` |
| `emitVecScalarOp` / `emitVecfScalarOp` | scalar op vec/vecf | 同上结构，标量广播到每个元素；vecf 标量侧先 `implConvert` 到 `FloatType` |
| `emitVecCopy` / `emitVecfCopy` | dst = src | **先 `store src.len, dst[0]` 覆盖 dst 长度**，再循环 `load src[i+1]` → `store dst[i+1]` |
| `visitStmt` (`lVal ASSIGN exp`) | 向量赋值 | lhs/rhs 都 `isVecValue` → `emitVecCopy`；都 `isVecfValue` → `emitVecfCopy`；否则原标量 store |
| `visitLVal` | 向量索引 | `vec[i]` 走 `emitVecElemPtr`、`vecf[i]` 走 `emitVecfElemPtr`（跳过 `[0]`），结果直接是 i32/float 元素指针 |
| `visitPrimaryExp` | 不衰减分支 | vec/vecf 裸名保留 `alloca [N x T]*` 类型，让 `visitAddExp/visitMulExp` 识别为向量 |
| `visitAddExp` | 向量判别分支 | 任一操作数是 vec → `emitVecBinOp/emitVecScalarOp`；任一是 vecf → `emitVecfBinOp/emitVecfScalarOp`（FADD/FSUB）；否则原标量路径 |
| `visitMulExp` | 向量判别分支 | 同上；vecf 走 `FMUL/FDIV`（`%` 走 `SREM`，vecf 不支持 `%`，占位） |

### 2.5 `src/antlr/*` — ANTLR 4.13.1 重新生成（对齐云端 runtime）

由 `scripts/grammar/regen_antlr_4131.sh` 自动生成。校验：
- `SysY2022Parser.h` 含 `VecDeclContext` 与 `VecfDeclContext`
- `SysY2022ParserVisitor.h` 含 `visitVecDecl` 与 `visitVecfDecl`
- `SysY2022Lexer.cpp` 含 `antlr4::internal`（4.13.1 标记）

### 2.6 测试用例与脚本

- `test/functional/vec_dynamic/01..07_*.sy` + `.out`：7 个整数变长向量测试用例
- `test/functional/vec_dynamic/08..15_vecf*.sy` + `.out`：8 个浮点变长向量测试用例
- `scripts/run_vec_dyn_tests.sh`：变长向量测试运行器（同时覆盖 vec / vecf）
- `scripts/grammar/regen_antlr_4131.sh`：ANTLR 4.13.1 重新生成脚本

---

## 三、关键注意要点（精简，仅保留重要坑点）

### 3.1 【严重】`visitPrimaryExp` 不能让 vec 裸名衰减

原代码对所有数组都做 `GEP [0, 0]` 衰减为首元素指针。这会让 `a + b` 里 `a` 变成
`i32*` 而非 `[VEC_MAX+1 x i32]*`，`isVecValue` 集合查找失败，向量运算符被当成标量加。

**修复**：在 `visitPrimaryExp` 加 `if (isVecValue(ptr)) return ptr;` 守卫，vec 裸名
直接返回 `alloca` 指针；普通数组照常衰减。判据是 `vecAllocas.count(ptr) > 0`。

### 3.2 【严重】`emitVecCopy` 必须先用 src 长度覆盖 dst 的 `[0]` 槽

`vec c;` 声明时 `emitVecAlloca(0)` 把长度 0 写入 `c[0]`。如果 `c = a + b;` 走
`emitVecCopy(c, rhs)` 时只循环拷贝数据不更新长度，`c[0]` 仍是 0，后续循环读取时
长度为 0 → 空循环 → 数据丢失。

**修复**：`emitVecCopy` 第一步 `store emitVecLen(src), dst[0]`（dst 长度由 src 决定），
再进入逐元素循环。这是变长版独有的关键不变量：**赋值后 dst 长度 = src 长度**。

### 3.3 【严重】`emitVecAlloca` 必须显式写长度 n 到 `[0]`

栈上 `alloca` 不会自动清零。如果只 `alloca` 不 `store n, [0]`，`[0]` 是垃圾值，
`emitVecLen` 读出来是随机数，循环次数完全失控。

**修复**：`emitVecAlloca(n)` 内部紧跟 `gep [0, 0]` + `store n`。这是变长版与定长版
的最大区别——定长版长度藏在 IR 类型 `[N x i32]` 里，变长版长度是运行时数据。

### 3.4 【中等】ANTLR 4.13.1 必须对齐云端 runtime

云端 runtime 是 4.13.1。若仍用旧 4.10.1 生成器，会引用 `antlr4::internal::OnceFlag`
等不存在符号，编译期报几百个错。

**修复**：在 `Ubuntu-24.04-eval` VM 中装 4.13.1 生成器 jar + 4.13.1 C++ runtime，
用 `scripts/grammar/regen_antlr_4131.sh` 重新生成 `src/antlr/`。

### 3.5 【中等】`vec c = a + b;` 中 rhs 长度可能非常量

`emitVecBinOp` 返回临时 vec，其 `[0]` 是 `load` 出来的运行时值。`visitVecDecl` Case 3
若直接 `emitVecAlloca(rhsLen)` 会失败（`emitVecAlloca` 签名是 `unsigned n`，编译期常量）。

**修复**：`visitVecDecl` Case 3 先 `dynamic_cast<ConstantInt*>(rhsLen)` 取编译期值；
若失败（运行时值），`emitVecAlloca(0)` 占位，再用运行时 `store rhsLen, [0]` 覆盖。

### 3.6 【低】测试 stdout 被 sylib 的 `TOTAL:` 行污染

`SysYlib/sylib.c` 的 `after_main` destructor 把计时信息写到 stderr。
`out=$($QEMU "$bin" 2>&1)` 把它合进 stdout，导致与 `.out` 期望不符。

**修复**：测试脚本改用 `out=$($QEMU "$bin" 2>/dev/null)`，丢弃 stderr。

### 3.7 【低】`04_vec_scalar` 期望值手算出错

`1 - a`（标量在左）= `{1-1, 1-2, 1-3, 1-4}` = `{0, -1, -2, -3}`，**不是** `{2, 1, 0, -1}`。
后者是误把 `1 - a` 当成 `a - 1` 算的。

**修复**：手算一遍校正 `.out`。**写期望值前务必手算**，不要凭直觉。

### 3.8 【已支持】浮点向量扩展（vecf）

本版在 `isVecValue` / `vecAllocas` 之外新增 `isVecfValue` / `vecfAllocas` 侧集合，
按 `alloca [VECF_MAX+1 x float]` 注册的 vecf 识别。元素类型改为 `FloatType`，
运算 opcode 改为 `FADD/FSUB/FMUL/FDIV`，循环骨架与整数版完全对称。
`[0]` 长度槽仍用 `i32` 存储，与元素类型解耦。两个侧集合互不重叠，
`visitAddExp` / `visitMulExp` 优先匹配 vec，未命中再匹配 vecf，最后落标量路径。

---

## 四、支持的语法形态总览

| 语法 | 语义 | IR 展开 | 实现位置 |
|------|------|---------|----------|
| `vec a;` | 空向量（长度 0） | `emitVecAlloca(0)`：`alloca [VEC_MAX+1 x i32]` + `store 0, [0]` | `visitVecDecl` Case 1 |
| `vec a = {1, 2, 3, 4};` | 列表初始化，长度 = 元素数 | `emitVecAlloca(4)` + 4 个 `store` 到 `[1..4]` | `visitVecDecl` Case 2 |
| `vec c = a + b;` | 表达式初始化，rhs 须为向量 | 求 rhs → `emitVecAlloca(len)` → `emitVecCopy` | `visitVecDecl` Case 3 |
| `c = a + b;`（声明后赋值） | 向量赋值 | `emitVecCopy(lhs, rhs)`：先覆盖 `[0]` 长度，再逐元素 `load/store` | `visitStmt` + `emitVecCopy` |
| `vec c = a * 2;` / `3 + a` | 标量广播 | `emitVecScalarOp`：运行时循环，标量与每个元素做标量 op | `emitVecScalarOp` |
| `c[i]` | 向量索引 | `emitVecElemPtr`：`gep [0, i+1]`（跳过 `[0]` 长度槽） | `visitLVal` |
| `a + b` / `a - b` / `a * b` / `a / b` / `a % b` | 向量二元运算 | 运行时循环：`alloca i` + cond/body/end 三块 | `visitAddExp` / `visitMulExp` + `emitVecBinOp` |
| `vecf a;` | 空浮点向量（长度 0） | `emitVecfAlloca(0)`：`alloca [VECF_MAX+1 x float]` + `store 0, [0]` | `visitVecfDecl` Case 1 |
| `vecf a = {1.0, 2.0, 3.0, 4.0};` | 列表初始化，长度 = 元素数 | `emitVecfAlloca(4)` + 4 个 `store` float 到 `[1..4]` | `visitVecfDecl` Case 2 |
| `vecf c = a + b;` | 浮点表达式初始化 | 求 rhs → `emitVecfAlloca(len)` → `emitVecfCopy` | `visitVecfDecl` Case 3 |
| `c = a + b;`（vecf 声明后赋值） | 浮点向量赋值 | `emitVecfCopy(lhs, rhs)`：先覆盖 `[0]` 长度，再逐元素 `load/store` float | `visitStmt` + `emitVecfCopy` |
| `vecf c = a * 2.0;` / `3.0 + a` | 浮点标量广播 | `emitVecfScalarOp`：循环，标量侧先 `implConvert` 到 `FloatType` | `emitVecfScalarOp` |
| `c[i]`（c 是 vecf） | 浮点向量索引 | `emitVecfElemPtr`：`gep [0, i+1]`（跳过 `[0]`，元素类型 float） | `visitLVal` |
| `a + b` / `a - b` / `a * b` / `a / b`（vecf 二元） | 浮点向量二元运算 | 运行时循环，opcode 为 `FADD/FSUB/FMUL/FDIV` | `visitAddExp` / `visitMulExp` + `emitVecfBinOp` |

支持运算符：`+ - * / %`（`/` 和 `%` 是 `SDIV / SREM`，非 SIMD 求余）；
浮点向量 `vecf` 支持 `+ - * /`（`FADD/FSUB/FMUL/FDIV`），不支持 `%`（占位走 `SREM`）。

---

## 五、测试用例

新建 `test/functional/vec_dynamic/` 子目录，15 个用例覆盖所有路径（7 整数 + 8 浮点）：

### 5.1 整数变长向量（7 个）

| 用例 | 覆盖点 | 期望输出 |
|------|--------|----------|
| `01_vec_add.sy` | `vec + vec`（基础） | `5 5 5 5` |
| `02_vec_sub.sy` | `vec - vec` | `-3 -1 1 3` |
| `03_vec_mul.sy` | `vec * vec`（逐元素） | `4 6 6 4` |
| `04_vec_scalar.sy` | `vec * k`、`k + vec`、`vec * 10`、`k - vec` | 4 行广播结果 |
| `05_vec_assign.sy` | `vec c; c = a + b;`（visitStmt 路径 + emitVecCopy 覆盖长度） | `5 5 5 5` |
| `06_vec_empty.sy` | `vec a; vec b = {1,2,3}; a = b;`（空声明 + 赋值，变长独有路径） | `1 2 3` |
| `07_vec_chain.sy` | `a + b + c` 链式（`emitVecBinOp` 返回临时 vec 再做 +） | `8 8 8 8` |

### 5.2 浮点变长向量（8 个）

| 用例 | 覆盖点 | 期望输出 |
|------|--------|----------|
| `08_vecf_add.sy` | `vecf + vecf`（FADD 循环） | `5 6 7 8` |
| `09_vecf_sub.sy` | `vecf - vecf`（FSUB 循环） | `3 2 1 0` |
| `10_vecf_mul.sy` | `vecf * vecf`（FMUL 循环） | `2 4 6 8` |
| `11_vecf_scalar.sy` | `vecf * k`、`k + vecf`、`vecf * 10`、`k - vecf`（标量广播） | 4 行广播结果 |
| `12_vecf_assign.sy` | `vecf f; f = a + b;`（visitStmt 浮点路径 + emitVecfCopy 覆盖长度） | `5 6 7 8` |
| `13_vecf_empty.sy` | `vecf e;`（空浮点向量，循环 0 次安全退化） | `99` |
| `14_vecf_chain.sy` | `a + b*c` + `d + a` + `e + d` 链式（`emitVecfBinOp` 返回临时 vecf 再做 +） | `15 18 21 24` |
| `15_vecf_div.sy` | `vecf / k`（FDIV 循环，`putint` 截断整数结果） | `1 2 3 4` |

每个 `.sy` 都配 `.out` 文件（**最后一行是返回码**，`run_vec_dyn_tests.sh` 用
`head -n -1` 剥离，剩下是期望 stdout）。浮点用例通过 `putint` 对 float 的隐式
截断避免 `putfloat` 的 `%a` 格式复杂性，整数结果便于断言。

测试脚本：`scripts/run_vec_dyn_tests.sh`：

```bash
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/run_vec_dyn_tests.sh O0"
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/run_vec_dyn_tests.sh O1"
```

**实测**：15/15 PASS @ O0，15/15 PASS @ O1（说明优化器对去语法糖后的运行时循环
IR 完全友好，整数与浮点对称）。功能测试回归 97/100 PASS @ O1（3 个 DIFF 是
`62_percolation` / `68_brainfk` / `71_full_conn`，均为合并基线即存在的预存回归，
非本次引入；O0 下 `71_full_conn` 通过，符合之前 A 组支配树 L/R + GVN 全支配引入的
已知缺陷）。

---

## 六、云端环境复现命令

VM：`Ubuntu-24.04-eval`（与云端对齐：clang++ 18.1.3 / ANTLR 4.13.1 /
riscv64-linux-gnu-gcc 13.3.0 / qemu-riscv64 8.2.2）。

```bash
# 1. 重新生成 ANTLR（4.13.1）
wsl -d Ubuntu-24.04-eval -e bash -lc "bash /mnt/d/VSCodeProjects/compiler/scripts/grammar/regen_antlr_4131.sh"

# 2. 构建 compiler（clang++ + 4.13.1 runtime）
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && rm -rf build && mkdir build && cd build && cmake -G 'Unix Makefiles' -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_COMPILER=clang++ .. && cmake --build . --target compiler -- -j8"

# 3. 构建 sylib
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/build_sylib.sh"

# 4. 跑变长向量测试
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/run_vec_dyn_tests.sh O0"
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/run_vec_dyn_tests.sh O1"

# 5. 回归（保证不破坏已有功能）
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/run_tests.sh quick O0"
```

---

## 七、与定长版的对照

| 维度 | 定长版（`feat-simd-vector`） | 变长版（`feat-vec-dynamic-fe`，本分支） |
|------|--------------------------|------------------------------------|
| vec 表示 | `alloca [N x i32]`（N 在类型里） | `alloca [VEC_MAX+1 x i32]`（`[0]` 存长度） |
| vecf 表示 | `alloca [N x float]` | `alloca [VECF_MAX+1 x float]`（`[0]` 存长度） |
| 长度决定 | 编译期常量（`vec a = {...}` 长度=元素数；`vec a;` 默认 4） | 运行时值（`vec a = {...}` 长度=元素数；`vec a;` 默认 0；`a = b;` 长度由 b 决定） |
| 运算展开 | **编译期展开**：N 个标量 `load/op/store` | **运行时循环**：`alloca i` + cond/body/end 三块 |
| vec/vecf 识别 | `PointerType<[N x i32/float]>` 类型判别 | `vecAllocas` / `vecfAllocas` 侧集合判别（类型相同） |
| `vec a;` / `vecf a;` 默认 | 长度 4，零初始化 | 长度 0（空向量） |
| 栈占用 | `4*N` 字节（N 通常 4，约 16 字节） | `4*(VEC_MAX+1) = 4100` 字节（固定，整数/浮点相同） |
| 优化器友好度 | N 个标量指令，LoopUnroll 无作用（已展开） | 循环结构，LoopUnroll / SCCP / Mem2Reg 都能优化 |
| 测试用例数 | 7 个（`simd_vector/01..07`）+ 7 个 vecf4（`simd_vector/09..15`） | 7 个整数 + 8 个浮点（`vec_dynamic/01..15`） |
| 标注 | `《前端+定长》` | `《前端+变长》` |

两版**完全互斥**（标注不同，可并存于不同分支）。最终决赛选用哪版取决于 FPGA 是否
支持 SIMD：若支持，定长版可后续接 RVV 后端；若不支持，变长版用运行时循环更灵活
（支持声明后改长度等动态语义）。

---

## 八、扩展空间

1. **真 SIMD 寄存器打包**：新增 `VectorType`（如 `<4 x i32>` / `<4 x float>`），TargetCodeGen 把
   连续 4 个 `load + add + store` 合并成 `vle32.v + vadd.vv + vse32.v`（需 RVV）。
   属于"后端"改动，**不在本版范围内**。
2. **运行时长度查询语法**：`len(a)` 直接读 `a[0]`。当前需用户自己 `putint(a[0])`。
3. **更丰富的元素类型**：当前支持 `i32`（vec）与 `float`（vecf）。可继续扩展
   `veci64`（i64）等：复制 `visitVecfDecl` 的对称结构，替换 `IntegerType::I64` 等。
4. **向量内建函数**：`dot(a, b)`、`sum(a)`、`shuffle(a, b, mask)` 等归约/重排操作。
5. **堆分配变长向量**：当前用栈 `alloca [VEC_MAX+1]`，每个 vec / vecf 固定占 4100
   字节。多 vec 场景下可改为堆分配 + 数据指针 + 长度三元组（`feat-vector-dynamic`
   分支已探索）。

本版保持后端/优化器零侵入，为上述扩展保留了干净的接入点：只要在 IRBuilder 把
新语法去语法糖为标量 IR 或运行时循环 IR，无需触碰其他层。浮点向量扩展已通过
复制整数版骨架 + 替换元素类型/opcode 完成验证，证明接入点足够干净。

---

## 九、文件清单

| 文件 | 状态 | 说明 |
|------|------|------|
| `grammar/SysY2022Lexer.g4` | 修改 | 加 `VEC: 'vec';` 与 `VECF: 'vecf';`（标 `《前端+变长》`） |
| `grammar/SysY2022Parser.g4` | 修改 | 加 `vecDecl` / `vecInit` / `vecfDecl`（标 `《前端+变长》`） |
| `include/ir/IRBuilder.h` | 修改 | 加 `vecAllocas` / `vecfAllocas` 集合 + 14 个方法声明（标 `《前端+变长》`） |
| `src/ir/IRBuilder.cpp` | 修改 | 18 处实现与接线（均标 `《前端+变长》`） |
| `src/antlr/*` | 重新生成 | 4.13.1，对齐云端 runtime，含 `VecDeclContext` 与 `VecfDeclContext` |
| `test/functional/vec_dynamic/01..07_*.sy` + `.out` | 新增 | 7 个整数变长向量测试用例 |
| `test/functional/vec_dynamic/08..15_vecf*.sy` + `.out` | 新增 | 8 个浮点变长向量测试用例 |
| `scripts/run_vec_dyn_tests.sh` | 新增 | 变长向量测试运行器（同时覆盖 vec / vecf） |
| `scripts/grammar/regen_antlr_4131.sh` | 新增 | ANTLR 4.13.1 重新生成脚本 |
| `变长向量语法_前端+变长_实现说明.md` | 新增 | 本文档 |

---

## 十、定位微调点

需要微调本版实现时，用以下命令快速定位所有改动点：

```bash
# 列出所有源码改动
grep -rn '《前端+变长》' grammar/ include/ir/IRBuilder.h src/ir/IRBuilder.cpp

# 只看 IRBuilder.cpp 中的改动
grep -n '《前端+变长》' src/ir/IRBuilder.cpp

# 统计改动点数
grep -r '《前端+变长》' grammar/ include/ir/IRBuilder.h src/ir/IRBuilder.cpp | wc -l
```

切换到其他版本（如"后端 + 真SIMD"或"前端 + 定长"）时，换一个标注名
（如 `《后端+SIMD》` 或 `《前端+定长》`）即可并存。

int state[2];
int carry;

void step() {
    state[1] = state[1] + 1;
    carry = carry + 0;
}

int main() {
    int iterations = 3;
    while (iterations > 0) {
        state[0] = 0;
        step();
        iterations = iterations - 1;
    }
    putint(state[1]);
    putch(10);
    return 0;
}

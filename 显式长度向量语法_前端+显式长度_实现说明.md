# 显式长度向量语法实现说明（前端 + 显式长度）

> **分支**: `feat-vec-explicit-len`（基于 `main`）
> **本版范围**: **只改生成 IR 以前的部分**（词法 / 语法 / IRBuilder），
> **支持显式声明长度的向量**（`vec4`、`vec8`、`vec16`... 整数元素 + `vecf4`、`vecf8`... 浮点元素），
> 长度从关键字提取。
> **源码标记**: 所有与 `main` 不同的源码改动均以注释 `《前端+显式长度》` 标注，
> 通过 `grep -rn '《前端+显式长度》' grammar/ include/ src/` 可一键定位微调点。
>
> **核心约束**: 决赛 FPGA 不确定在 ISA 层面支持 SIMD 指令（如 RVV），所以本版
> 不引入新 IR 类型、不触后端代码生成与优化器；向量运算在 IR 生成阶段**去语法糖
> 为编译期展开的标量 `load/op/store`**，最终汇编是纯标量 RV64GC。
>
> **整数向量与浮点向量**：两套关键字 `VEC_N` / `VECF_N` 对称设计，
> 同一份代码骨架（声明、初始化、运算、拷贝）复制成 `visit*Decl` / `visit*Init`
> + `emit*BinOp` / `emit*ScalarOp` / `emit*Copy` 两套，仅 `IntegerType::I32` ↔ `FloatType::get()`、
> `ADD/SUB/MUL/SDIV/SREM` ↔ `FADD/FSUB/FMUL/FDIV` 之别，类型判别 `isVecValue`
> （`PointerToArrayOfI32`）↔ `isVecfValue`（`PointerToArrayOfFloat`）。
>
> **与定长版/变长版的区别**:
> - 定长版（`feat-simd-vector`）：`VEC: 'vec';`，长度由初始化列表推断，`vec a;` 默认 4
> - 变长版（`feat-vec-dynamic-fe`）：`VEC: 'vec';`，运行时长度，`alloca [VEC_MAX+1]` + 运行时循环
> - **显式长度版（本分支）**：`VEC_N: 'vec' DIGIT+;` / `VECF_N: 'vecf' DIGIT+;`，
>   **长度从关键字提取**（`vec4`→4，`vecf8`→8），`alloca [N x i32|float]` + **编译期展开** N 次标量运算

---

## 一、本版范围与设计哲学

### 1.1 用户语法目标

```c
vec4 a = {1, 2, 3, 4};     // 长度 4，从关键字 'vec4' 提取
vec4 b = {4, 3, 2, 1};
vec4 c = a + b;            // 编译期展开 4 次标量加法
vec4 d = a * 2;            // 标量广播，编译期展开 4 次
vec4 e = 3 + a;            // 标量在左
vec4 f;                    // 默认零初始化（4 个 0）
f = a + b;                 // 声明后赋值，emitVecCopy 编译期展开
vec8 g = {1,1,1,1,1,1,1,1}; // 长度 8，从关键字 'vec8' 提取
putint(c[0]);              // 标准数组索引

// 《前端+显式长度》 浮点向量扩展：vecf4 / vecf8 等
vecf4 fa = {1.0, 2.0, 3.0, 4.0};
vecf4 fb = {4.0, 3.0, 2.0, 1.0};
vecf4 fc = fa + fb;        // 编译期展开 4 次 FADD
vecf4 fd = fa * 2.0;       // 标量广播，编译期展开 4 次 FMUL
vecf4 fe = 1.0 - fa;       // 标量在左，FSUB
vecf4 ff;                  // 默认零初始化（4 个 0.0）
ff = fa + fb;              // 声明后赋值，emitVecfCopy 编译期展开
putint(fc[0]);             // putint 接受 float 参数（f→i 隐式转换）
```

### 1.2 "前端 + 显式长度" 的含义

| 关键词 | 含义 |
|--------|------|
| **前端** | 只动 `grammar/*.g4` + `include/ir/IRBuilder.h` + `src/ir/IRBuilder.cpp`，不动 `src/opt/*` 优化器、不动 `src/backend/*` 代码生成、不动 `include/ir/IR.h` 类型系统 |
| **显式长度** | 向量长度在**关键字中显式声明**：`vec4`、`vec8`、`vec16`、`vecf4`、`vecf8`... 长度由 `VEC_N` / `VECF_N` token 的数字部分提取（编译期常量），不依赖初始化列表元素数，不需要默认值 |
| **去语法糖** | `vecN` 在 IR 中表示 `alloca [N x i32]`，`vecfN` 表示 `alloca [N x float]`。所有向量运算符展开为**编译期展开**的 N 个标量 `load/op/store`（整数 `ADD/SUB/MUL/SDIV/SREM`，浮点 `FADD/FSUB/FMUL/FDIV`，同定长版，非运行时循环） |
| **类型识别** | vec 与普通 `[N x i32]` 数组、vecf 与普通 `[N x float]` 数组在 IR 中类型相同，靠 `isVecValue` / `isVecfValue` 的**类型结构判别**（`PointerToArrayOfI32` / `PointerToArrayOfFloat`）区分，无需侧集合 |

### 1.3 为什么这么设计

- **后端零改动**：FPGA BOOM 软核只保证 RV64GC，没有 RVV；纯标量 IR 走现有 TargetCodeGen 即可。
- **优化器零改动**：标量 IR + 编译期展开的标量指令，走 Mem2Reg / SCCP 等已有 pass，优化器对线性标量代码天然友好。
- **类型系统零改动**：不新增 `VectorType`，复用 `ArrayType` + `PointerType`。
- **长度任意**：`VEC_N: 'vec' DIGIT+;` 与 `VECF_N: 'vecf' DIGIT+;` 一条词法规则覆盖所有正整数长度（vec1, vec4, vec8, vec16, vec32, vec64... + vecf1, vecf4, vecf8...），**无需为每个长度新增关键字**。
- **整数 / 浮点对称**：浮点向量复用整数向量的代码骨架，复制成第二套 `visitVecf*` / `emitVecf*`，差异仅元素类型与 opcode；新增浮点向量未触后端 / 优化器，仅 IR 生成层增加约 150 行对称实现。
- **代价**：编译期展开意味着向量越长，生成的标量指令越多（vec4 → 4 条，vec8 → 8 条，vec16 → 16 条）。对于很长的向量，变长版的运行时循环更省代码体积。

---

## 二、源码改动清单（4 个手改文件 + 1 个脚本 + ANTLR 重新生成 + 测试用例，全部标 `《前端+显式长度》`）

> 检索命令：`grep -rn '《前端+显式长度》' grammar/ include/ir/IRBuilder.h src/ir/IRBuilder.cpp`
> 浮点扩展标注：`grep -rn '浮点向量扩展' grammar/ include/ir/IRBuilder.h src/ir/IRBuilder.cpp`

### 2.1 `grammar/SysY2022Lexer.g4` — 加 `VEC_N` / `VECF_N` 词法规则

```antlr
// 《前端+显式长度》 显式长度向量关键字（vec4/vec8/vec16/...）
// 放在 IDENTIFIER 前：max munch 下 'vec4' 长度 4 == IDENTIFIER 长度 4，
// 平局由规则顺序决定 → VEC_N 胜（成为关键字）；'vec4x' 长度 5 > VEC_N 的 4
// → IDENTIFIER 胜（避免误吞 vec4x 变量名）。长度任意正整数，无需为每个长度
// 新增关键字。
VEC_N:    'vec' DIGIT+;

// 《前端+显式长度》 浮点向量扩展：显式长度浮点向量关键字（vecf4/vecf8/...）
// 'vecf' 前缀 + DIGIT+，与 VEC_N 互斥（'vecf4' 的 'f' 非 DIGIT，VEC_N 不匹配）。
VECF_N:   'vecf' DIGIT+;
```

**设计要点**：`VEC_N` 放在 `IDENTIFIER` 之前。ANTLR max munch（最长匹配）规则下：
- `vec4`（4字符）== `IDENTIFIER` 匹配长度（4字符）→ 平局，`VEC_N` 先定义者胜 → 关键字
- `vec4x`（5字符）> `VEC_N` 的 `vec4`（4字符）→ `IDENTIFIER` 胜 → 变量名
- `vec`（无数字）→ `VEC_N` 不匹配 → `IDENTIFIER`
- `vector`（`t` 非数字）→ `VEC_N` 不匹配 → `IDENTIFIER`
- `vecf4`（5字符）：`VEC_N` 在 `'vec'` 后要求紧跟 `DIGIT+`，`'f'` 非 DIGIT → `VEC_N` 不匹配；`VECF_N` 的 `'vecf'` 前缀 + DIGIT 完整匹配 → `VECF_N` 胜（浮点关键字）
- `vecf4x`（6字符）> `VECF_N` 的 5 → `IDENTIFIER` 胜（变量名）

### 2.2 `grammar/SysY2022Parser.g4` — 加 `vecDecl` / `vecfDecl` / `vecInit` 规则

```antlr
// 《前端+显式长度》 在原 SysY 声明基础上新增 vecDecl 分支
// 浮点向量扩展：新增 vecfDecl 分支
decl: constDecl | varDecl | vecDecl | vecfDecl;

// 《前端+显式长度》 显式长度向量声明（vec4/vec8/vec16/...，长度从关键字提取）：
//   vec4 name = { e1, e2, e3, e4 };   列表初始化（长度由关键字决定，非列表元素数）
//   vec4 name = exp;                  表达式初始化（exp 须为同长度向量，如 a + b）
//   vec4 name;                        默认零初始化（长度由关键字决定）
// vecInit 以 L_BRACE 起首，exp 以 L_PAREN/IDENTIFIER/number/unaryOp 起首，
// 两者首 token 互斥，ANTLR LL(*) 无歧义。
// 不引入新 IR 类型；vecN 表示为 alloca [N x i32]，
// 运算编译期展开为 N 个标量 load/op/store（同定长版，非运行时循环）。
vecDecl: VEC_N IDENTIFIER (ASSIGN (vecInit | exp))? SEMICOLON;

// 《前端+显式长度》 向量初值（列表形式）
vecInit: L_BRACE exp (COMMA exp)* R_BRACE;

// 《前端+显式长度》 浮点向量扩展：显式长度浮点向量声明（vecf4/vecf8/...）
// 语法与 vecDecl 对称，区别仅在 VECF_N 关键字（float 元素）。
// vecfN 表示为 alloca [N x float]，运算用 FADD/FSUB/FMUL/FDIV。
vecfDecl: VECF_N IDENTIFIER (ASSIGN (vecInit | exp))? SEMICOLON;
```

### 2.3 `include/ir/IRBuilder.h` — 11 个辅助方法声明（整数向量 5 个 + 浮点向量 6 个）

```cpp
// 《前端+显式长度》 显式长度向量声明访问器（vec4/vec8/...，仅前端扩展）
std::any visitVecDecl(SysY2022Parser::VecDeclContext* ctx) override;
std::any visitVecInit(SysY2022Parser::VecInitContext* ctx) override;
// 《前端+显式长度》 浮点向量扩展：显式长度浮点向量声明访问器（vecf4/vecf8/...）
std::any visitVecfDecl(SysY2022Parser::VecfDeclContext* ctx) override;

// ===== 《前端+显式长度》 显式长度向量运算辅助（仅前端 IR 生成扩展） =====
// vecN 表示为 alloca [N x i32]：长度从关键字 N 提取（编译期常量）
// 运算编译期展开为 N 个标量 load/op/store（同定长版，非运行时循环）
// 判断 Value 是否为显式长度向量（PointerToArrayOfI32）
bool   isVecValue(Value* v);
// 从 VEC_N token 文本提取长度（"vec4" → 4）
unsigned extractVecLen(antlr4::tree::TerminalNode* vecToken);
// 向量二元运算（编译期展开）：left op right，返回结果 alloca [N x i32]
Value* emitVecBinOp(Instruction::Opcode op, Value* left, Value* right);
// 标量广播运算（编译期展开）：scalar op vec，返回结果 alloca [N x i32]
Value* emitVecScalarOp(Instruction::Opcode op, Value* scalar,
                       Value* vec, bool scalarOnLeft);
// 向量逐元素拷贝（编译期展开）：dst = src
void   emitVecCopy(Value* dst, Value* src);

// ===== 《前端+显式长度》 浮点向量扩展（FADD/FSUB/FMUL/FDIV，编译期展开） =====
// 判断 Value 是否为浮点向量（PointerToArrayOfFloat）
bool   isVecfValue(Value* v);
// 从 VECF_N token 文本提取长度（"vecf4" → 4）
unsigned extractVecfLen(antlr4::tree::TerminalNode* vecfToken);
// 浮点向量二元运算（编译期展开）：left op right → alloca [N x float]
Value* emitVecfBinOp(Instruction::Opcode op, Value* left, Value* right);
// 浮点标量广播运算（编译期展开）：scalar op vecf → alloca [N x float]
Value* emitVecfScalarOp(Instruction::Opcode op, Value* scalar,
                        Value* vec, bool scalarOnLeft);
// 浮点向量逐元素拷贝（编译期展开）：dst = src
void   emitVecfCopy(Value* dst, Value* src);
```

### 2.4 `src/ir/IRBuilder.cpp` — 实现与接线（整数向量 9 处 + 浮点向量 7 处标注）

**整数向量**：

| 函数 | 标注位置 | 作用 |
|------|----------|------|
| `visitDecl` | 分发分支 | 新增 `if (ctx->vecDecl()) return visitVecDecl(...)` |
| `visitVecDecl` Case 1 | 无初始化 | `alloca [N x i32]` + N 个 `store 0`（零初始化） |
| `visitVecDecl` Case 2 | 列表初始化 | `alloca [N x i32]` + 逐元素 `store`（不足补零，超出截断） |
| `visitVecDecl` Case 3 | 表达式初始化 | 求 rhs；若 rhs 是 vec，`emitVecCopy` 逐元素拷贝 |
| `visitVecInit` | 空 Visitor | 内联处理，保留空实现满足接口 |
| `isVecValue` | 判据 | 类型结构判别：`PointerType` → `ArrayType` → `IntegerType` |
| `extractVecLen` | 长度提取 | `VEC_N` token 文本 `substr(3)` → `stoul` |
| `emitVecBinOp` | vec op vec | `alloca [N x i32]` + N 次循环展开 `load l[i], load r[i], op, store d[i]` |
| `emitVecScalarOp` | scalar op vec | 同上结构，标量广播到每个元素 |
| `emitVecCopy` | dst = src | N 次循环展开 `load src[i]` → `store dst[i]` |
| `visitStmt` (`lVal ASSIGN exp`) | 向量赋值 | lhs/rhs 都 `isVecValue` → `emitVecCopy`，否则原标量 store |
| `visitPrimaryExp` | 不衰减分支 | vec 裸名保留 `alloca [N x i32]*` 类型，让 `visitAddExp/visitMulExp` 识别为向量 |
| `visitAddExp` | 向量判别分支 | 任一操作数是向量 → 走 `emitVecBinOp` 或 `emitVecScalarOp`；否则原标量路径 |
| `visitMulExp` | 向量判别分支 | 同上，`%` 走 `SREM` 不走 `FREM` |

**浮点向量**（与整数向量对称，复用同一套代码骨架，差异标注）：

| 函数 | 标注位置 | 作用 |
|------|----------|------|
| `visitDecl` | 浮点分发分支 | 新增 `if (ctx->vecfDecl()) return visitVecfDecl(...)` |
| `visitVecfDecl` Case 1 | 无初始化 | `alloca [N x float]` + N 个 `store 0.0`（零初始化） |
| `visitVecfDecl` Case 2 | 列表初始化 | `alloca [N x float]` + 逐元素 `store`（不足补零，超出截断，元素 `implConvert` 到 `FloatType`） |
| `visitVecfDecl` Case 3 | 表达式初始化 | 求 rhs；若 rhs 是 vecf，`emitVecfCopy` 逐元素拷贝 |
| `isVecfValue` | 浮点判据 | 类型结构判别：`PointerType` → `ArrayType` → `FloatType` |
| `extractVecfLen` | 长度提取 | `VECF_N` token 文本 `substr(4)` → `stoul`（去掉 `"vecf"` 前缀） |
| `emitVecfBinOp` | vecf op vecf | `alloca [N x float]` + N 次循环展开 `FADD/FSUB/FMUL/FDIV` |
| `emitVecfScalarOp` | scalar op vecf | 同上结构，标量先 `implConvert` 到 `FloatType` 后广播 |
| `emitVecfCopy` | dst = src | N 次循环展开 `load src[i]` → `store dst[i]`（浮点 load/store） |
| `visitStmt` (`lVal ASSIGN exp`) | 浮点向量赋值 | lhs/rhs 都 `isVecfValue` → `emitVecfCopy` |
| `visitPrimaryExp` | 浮点不衰减分支 | vecf 裸名保留 `alloca [N x float]*` 类型 |
| `visitAddExp` | 浮点向量判别分支 | 任一操作数是 vecf → `FADD`/`FSUB`；标量侧通过 `implConvert` 转 `FloatType` |
| `visitMulExp` | 浮点向量判别分支 | `*`→`FMUL`、`/`→`FDIV`、`%`→`SREM`（与标量浮点路径一致，无 `FREM` opcode） |

### 2.5 `src/antlr/*` — ANTLR 4.13.1 重新生成（对齐云端 runtime）

由 `scripts/grammar/regen_antlr_4131.sh` 自动生成。校验：
- `SysY2022Parser.h` 含 `VecDeclContext` 与 `VecfDeclContext`
- `SysY2022ParserVisitor.h` 含 `visitVecDecl` 与 `visitVecfDecl`
- `SysY2022Lexer.h` 含 `VEC_N` / `VECF_N` token（token number ≥ 1）
- `SysY2022Lexer.cpp` 含 `antlr4::internal`（4.13.1 标记）

### 2.6 测试用例与脚本

- `test/functional/vec_explicit/01..08_*.sy` + `.out`：8 个整数向量测试用例
- `test/functional/vec_explicit/09..15_*.sy` + `.out`：7 个浮点向量测试用例（标 `《前端+显式长度》 浮点向量扩展`）
- `scripts/run_vec_explicit_tests.sh`：显式长度向量测试运行器（同时覆盖整数与浮点用例）
- `scripts/grammar/regen_antlr_4131.sh`：ANTLR 4.13.1 重新生成脚本

---

## 三、如何添加新的长度支持

**结论：不需要任何修改**。本版使用 `VEC_N: 'vec' DIGIT+;` 和 `VECF_N: 'vecf' DIGIT+;` 两条词法规则，
一条规则覆盖所有正整数长度。`vec1`、`vec4`、`vec8`、`vec16`、`vec32`、`vec64`... 和 `vecf1`、`vecf4`、`vecf8`、
`vecf16`... 全部自动支持，无需为每个长度新增关键字或修改 IR 生成代码。

### 3.1 长度提取机制

长度在 `IRBuilder::extractVecLen` / `extractVecfLen` 中从 token 文本提取：

```cpp
unsigned IRBuilder::extractVecLen(tree::TerminalNode* vecToken) {
    std::string text = vecToken->getText();   // "vec4" / "vec8" / "vec16"
    return static_cast<unsigned>(std::stoul(text.substr(3)));  // 去掉 "vec" 前缀
}

unsigned IRBuilder::extractVecfLen(tree::TerminalNode* vecfToken) {
    std::string text = vecfToken->getText();   // "vecf4" / "vecf8" / "vecf16"
    return static_cast<unsigned>(std::stoul(text.substr(4)));  // 去掉 "vecf" 前缀
}
```

`text.substr(3)` / `text.substr(4)` 去掉前缀，`stoul` 解析剩余数字。`vec4` → 4，`vec16` → 16，`vecf8` → 8。

### 3.2 长度传播机制

长度提取后，通过 `ArrayType::get(IntegerType::I32, len)` / `ArrayType::get(FloatType::get(), len)` 嵌入 IR 类型：
- `vec4 a;` → `alloca [4 x i32]`
- `vec8 b;` → `alloca [8 x i32]`
- `vecf4 fa;` → `alloca [4 x float]`
- `vecf8 fb;` → `alloca [8 x float]`

所有后续运算（`emitVecBinOp` / `emitVecfBinOp` 等）从操作数的
`ArrayType::getNumElements()` 读取长度，自动展开对应次数。

### 3.3 如果要限制为特定长度（可选）

如果不希望支持任意长度（例如只允许 vec4 和 vec8），在 `visitVecDecl` / `visitVecfDecl` 开头加校验：

```cpp
std::any IRBuilder::visitVecDecl(SysY2022Parser::VecDeclContext* ctx) {
    unsigned len = extractVecLen(ctx->VEC_N());
    // ===== 只允许 vec4 和 vec8 =====
    if (len != 4 && len != 8) {
        throw std::runtime_error("Unsupported vec length: " + std::to_string(len)
                                 + ". Only vec4 and vec8 are supported.");
    }
    // ... 原有逻辑
}
```

### 3.4 浮点向量已实现（本版即支持）

整数向量与浮点向量在本版**同时支持**，对称设计，无须额外步骤。要新增 `vec4f` 之类命名的
变体（如希望 `'4f'` 后缀而非 `'vecf'` 前缀），只需调整词法规则前缀：

```antlr
// 替代方案：'vec4f' / 'vec8f' 形式（后缀 'f'）
VECF_N: 'vec' DIGIT+ 'f';
```

然后 `extractVecfLen` 改为去掉首尾各 1 字符：`text.substr(4).substr(0, text.size()-5)`，
或保留 `'vecf'` 前缀的当前设计（更易读、更易扩展）。当前实现的 `'vecf' DIGIT+` 已被验证可行。

### 3.5 如果要添加新的元素类型（如 double / i64）

当前已支持 `i32`（`VEC_N`）和 `float`（`VECF_N`）。要扩展其他元素类型，复制对称模板即可：

1. **词法**：新增 `VECD_N: 'vecd' DIGIT+;`（double 元素）
2. **语法**：`vecdDecl: VECD_N IDENTIFIER (ASSIGN (vecInit | exp))? SEMICOLON;`，在 `decl` 加分支
3. **IR 生成**（IRBuilder.h + IRBuilder.cpp）：
   - `visitVecdDecl` 中用 `ArrayType::get(DoubleType::get(), len)`
   - `emitVecdBinOp` 中用 `DoubleType::get()` + 对应 opcode（FADD 等复用，LLVM 风格浮点 opcode 不区分精度）
   - `isVecdValue` 检查 `ArrayType` 的元素类型是 `DoubleType`
4. **运算分发**：`visitAddExp` / `visitMulExp` 增加 `isVecdValue` 分支

### 3.6 如果要支持非常大的长度（如 vec1024）

编译期展开 `vec1024` 会生成 1024 条标量 `load/op/store`，代码体积爆炸。
此时建议：
1. 改用变长版（`feat-vec-dynamic-fe`）的运行时循环方案
2. 或在 `emitVecBinOp` 中对 `len > 阈值`（如 32）的情况改用运行时循环

---

## 四、关键注意要点（精简，仅保留重要坑点）

### 4.1 【严重】`visitPrimaryExp` 不能让 vec / vecf 裸名衰减

原代码对所有数组都做 `GEP [0, 0]` 衰减为首元素指针。这会让 `a + b` 里 `a` 变成
`i32*` / `float*` 而非 `[N x i32]*` / `[N x float]*`，`isVecValue` / `isVecfValue` 类型判据失败，
向量运算符被当成标量加。

**修复**：在 `visitPrimaryExp` 加 `if (isVecValue(ptr)) return ptr;` 和
`if (isVecfValue(ptr)) return ptr;` 守卫，vec / vecf 裸名直接返回 `alloca` 指针；
普通数组照常衰减。判据是 `PointerType → ArrayType → IntegerType/FloatType`。

### 4.2 【严重】ANTLR max munch 与 IDENTIFIER 的冲突

`VEC_N: 'vec' DIGIT+;` 和 `VECF_N: 'vecf' DIGIT+;` 必须放在 `IDENTIFIER` 之前。
ANTLR max munch 下：
- `vec4`（4字符）== `IDENTIFIER`（4字符）→ 平局，规则顺序决定 → `VEC_N` 先定义者胜
- `vec4x`（5字符）> `VEC_N` 的 `vec4`（4字符）→ `IDENTIFIER` 胜（正确，`vec4x` 是变量名）
- `vecf4`（5字符）：`VEC_N` 在 `'vec'` 后要求紧跟 `DIGIT+`，`'f'` 非 DIGIT → `VEC_N` 不匹配；
  `VECF_N` 的 `'vecf'` 前缀 + DIGIT 完整匹配 → `VECF_N` 胜（浮点关键字）
- `vecf4x`（6字符）> `VECF_N` 的 5 → `IDENTIFIER` 胜（变量名）

**如果顺序反了**：`vec4` / `vecf4` 会被 `IDENTIFIER` 匹配（平局时 `IDENTIFIER` 先定义者胜），
向量语法完全不生效。

### 4.3 【严重】ANTLR 4.13.1 必须对齐云端 runtime

云端 runtime 是 4.13.1。若仍用旧 4.10.1 生成器，会引用 `antlr4::internal::OnceFlag`
等不存在符号，编译期报几百个错。

**修复**：在 `Ubuntu-24.04-eval` VM 中装 4.13.1 生成器 jar + 4.13.1 C++ runtime，
用 `scripts/grammar/regen_antlr_4131.sh` 重新生成 `src/antlr/`。

### 4.4 【中等】列表初始化元素数 ≠ 关键字长度

`vec4 a = {1, 2};`（2 个元素，长度 4）：剩余 2 个元素零初始化。
`vec4 a = {1, 2, 3, 4, 5};`（5 个元素，长度 4）：第 5 个元素被截断。
`vecf4 a = {1.0, 2.0};`（2 个元素，长度 4）：剩余 2 个元素零初始化（`0.0`）。

**实现**：`visitVecDecl` / `visitVecfDecl` Case 2 中 `for (i = 0; i < len; ++i)`，
`i < elems.size()` 时取列表元素，否则取 `ConstantInt::get(0)` / `ConstantFloat::get(0.0)`。

### 4.5 【中等】浮点向量测试输出格式

`SysYlib/sylib.c` 的 `putfloat` 使用 `%a` 十六进制浮点格式（如 `0x1.8p+2`），
不便人工比对期望值。整数向量用 `putint` 直接输出十进制整数；浮点向量同样可用
`putint`，因为 `putint` 接受 `float` 参数（f→i 隐式转换，截断到零）。
**条件**：浮点值是整数（如 `5.0`、`-3.0`）时，`putint` 输出与整数完全一致，
适合做基准测试。非整数结果请用 `putfloat` 配 `%a` 期望值（如 `0x1.8p+2`）。

### 4.6 【低】测试 stdout 被 sylib 的 `TOTAL:` 行污染

`SysYlib/sylib.c` 的 `after_main` destructor 把计时信息写到 stderr。
`out=$($QEMU "$bin" 2>&1)` 把它合进 stdout，导致与 `.out` 期望不符。

**修复**：测试脚本改用 `out=$($QEMU "$bin" 2>/dev/null)`，丢弃 stderr。

### 4.7 【低】测试期望值手算出错

`1 - a`（标量在左）= `{1-1, 1-2, 1-3, 1-4}` = `{0, -1, -2, -3}`，**不是** `{2, 1, 0, -1}`。
`vec4 a;` 后 `a + b`（b={4,3,2,1}）= `{0+4, 0+3, 0+2, 0+1}` = `{4, 3, 2, 1}`，**不是** `{5, 5, 5, 5}`。
浮点向量 `vecf4 a = {1.0, 2.0, 3.0, 4.0}; a / 2.0` = `{0.5, 1.0, 1.5, 2.0}`，
`putint` 截断为 `{0, 1, 1, 2}`，**不是** `{2, 1, 0, -1}`（误把 `a/2` 当成 `2/a`）。

**修复**：手算一遍校正 `.out`。**写期望值前务必手算**，不要凭直觉。

---

## 五、支持的语法形态总览

| 语法 | 语义 | IR 展开 | 实现位置 |
|------|------|---------|----------|
| `vec4 a;` | 默认零初始化（长度 4） | `alloca [4 x i32]` + 4 个 `store 0` | `visitVecDecl` Case 1 |
| `vec4 a = {1, 2, 3, 4};` | 列表初始化 | `alloca [4 x i32]` + 4 个 `store` | `visitVecDecl` Case 2 |
| `vec4 a = {1, 2};` | 部分初始化 | `alloca [4 x i32]` + `store 1, store 2, store 0, store 0` | `visitVecDecl` Case 2 |
| `vec4 c = a + b;` | 表达式初始化 | 求 rhs → `emitVecCopy` | `visitVecDecl` Case 3 |
| `c = a + b;`（声明后赋值） | 向量赋值 | `emitVecCopy(lhs, rhs)`：N 次 `load/store` | `visitStmt` + `emitVecCopy` |
| `vec4 c = a * 2;` / `3 + a` | 标量广播 | `emitVecScalarOp`：N 次展开 | `emitVecScalarOp` |
| `c[i]` | 向量索引 | 标准 `GEP [0, i]`（与普通数组相同） | `visitLVal` |
| `a + b` / `a - b` / `a * b` / `a / b` / `a % b` | 向量二元运算 | 编译期展开 N 次 | `visitAddExp` / `visitMulExp` + `emitVecBinOp` |
| `vecf4 fa;` | 浮点默认零初始化 | `alloca [4 x float]` + 4 个 `store 0.0` | `visitVecfDecl` Case 1 |
| `vecf4 fa = {1.0, 2.0, 3.0, 4.0};` | 浮点列表初始化 | `alloca [4 x float]` + 4 个 `store` | `visitVecfDecl` Case 2 |
| `vecf4 fa = {1.0, 2.0};` | 浮点部分初始化 | `store 1.0, store 2.0, store 0.0, store 0.0` | `visitVecfDecl` Case 2 |
| `vecf4 fc = fa + fb;` | 浮点表达式初始化 | 求 rhs → `emitVecfCopy` | `visitVecfDecl` Case 3 |
| `fc = fa + fb;`（声明后赋值） | 浮点向量赋值 | `emitVecfCopy(lhs, rhs)`：N 次浮点 `load/store` | `visitStmt` + `emitVecfCopy` |
| `vecf4 fc = fa * 2.0;` / `1.0 - fa` | 浮点标量广播 | `emitVecfScalarOp`：N 次展开 | `emitVecfScalarOp` |
| `fa + fb` / `fa - fb` / `fa * fb` / `fa / fb` | 浮点向量二元运算 | 编译期展开 N 次 `FADD/FSUB/FMUL/FDIV` | `visitAddExp` / `visitMulExp` + `emitVecfBinOp` |

支持整数运算符：`+ - * / %`（`/` 和 `%` 是 `SDIV / SREM`）。
支持浮点运算符：`+ - * /`（`FADD / FSUB / FMUL / FDIV`），`%` 走 `SREM`（与标量浮点路径一致，无 `FREM` opcode）。
支持长度：任意正整数（`vec1`, `vec4`, `vec8`, `vec16`, `vec32`, ... + `vecf1`, `vecf4`, `vecf8`, `vecf16`, ...）。

---

## 六、测试用例

新建 `test/functional/vec_explicit/` 子目录，15 个用例（8 个整数 + 7 个浮点）覆盖所有路径：

**整数向量**（`01..08`）：

| 用例 | 覆盖点 | 期望输出 |
|------|--------|----------|
| `01_vec4_add.sy` | `vec4 + vec4`（基础加法） | `5 5 5 5` |
| `02_vec4_scalar.sy` | `vec4 * k`、`k + vec4`、`vec4 * 10`、`k - vec4` | 4 行广播结果 |
| `03_vec4_default.sy` | `vec4 a;`（零初始化）→ `a = a + b`（声明后赋值） | `0 0 0 0` / `4 3 2 1` |
| `04_vec4_chain.sy` | `a + b + c` 链式（`emitVecBinOp` 返回临时 vec4 再做 +） | `8 8 8 8` |
| `05_vec8_add.sy` | `vec8 + vec8`（验证 vec8 长度提取 + 8 次展开） | `9 9 9 9 9 9 9 9` |
| `06_vec8_scalar.sy` | `vec8 * 2` 标量广播 + `vec8 b; b = a` 声明后赋值 | 2 行 8 元素结果 |
| `07_vec4_mul.sy` | `vec4 - vec4` + `vec4 * vec4`（多运算符混合） | `-3 -1 1 3` / `4 6 6 4` |
| `08_vec4_partial.sy` | `vec4 a = {1, 2};`（部分初始化，剩余补零） | `1 2 0 0` |

**浮点向量**（`09..15`，标 `《前端+显式长度》 浮点向量扩展`，期望输出走 `putint` 隐式 f→i 转换）：

| 用例 | 覆盖点 | 期望输出 |
|------|--------|----------|
| `09_vecf4_add.sy` | `vecf4 + vecf4`（FADD 基础加法） | `5 5 5 5` |
| `10_vecf4_scalar.sy` | `vecf4 * k` / `k + vecf4` / `vecf4 * 10` / `k - vecf4` / `vecf4 / k`（FADD/FSUB/FMUL/FDIV 广播） | 5 行广播结果 |
| `11_vecf4_default.sy` | `vecf4 fa;`（零初始化）→ `fa = fa + fb`（声明后赋值 `emitVecfCopy`） | `0 0 0 0` / `4 3 2 1` |
| `12_vecf4_chain.sy` | `fa + fb + fc` 链式（`emitVecfBinOp` 返回临时 vecf4 再做 +） | `8 8 8 8` |
| `13_vecf4_mul.sy` | `vecf4 - vecf4` + `vecf4 * vecf4`（FSUB/FMUL 混合） | `-3 -1 1 3` / `4 6 6 4` |
| `14_vecf8_add.sy` | `vecf8 + vecf8`（验证 vecf8 长度提取 + 8 次展开） | `9 9 9 9 9 9 9 9` |
| `15_vecf4_partial.sy` | `vecf4 a = {1.0, 2.0};`（部分初始化，剩余补零 `0.0`） | `1 2 0 0` |

每个 `.sy` 都配 `.out` 文件（**最后一行是返回码**，`run_vec_explicit_tests.sh` 用
`head -n -1` 剥离，剩下是期望 stdout）。

测试脚本：`scripts/run_vec_explicit_tests.sh`：

```bash
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/run_vec_explicit_tests.sh O0"
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/run_vec_explicit_tests.sh O1"
```

**实测**：15/15 PASS @ O0，15/15 PASS @ O1（说明优化器对编译期展开后的标量 IR 完全友好，
整数与浮点向量皆然）。quick smoke 回归 5/5 PASS @ O0 和 O1（未破坏已有功能）。

---

## 七、云端环境复现命令

VM：`Ubuntu-24.04-eval`（与云端对齐：clang++ 18.1.3 / ANTLR 4.13.1 /
riscv64-linux-gnu-gcc 13.3.0 / qemu-riscv64 8.2.2）。

```bash
# 1. 重新生成 ANTLR（4.13.1）
wsl -d Ubuntu-24.04-eval -e bash -lc "bash /mnt/d/VSCodeProjects/compiler/scripts/grammar/regen_antlr_4131.sh"

# 2. 构建 compiler（clang++ + 4.13.1 runtime）
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && rm -rf build && mkdir build && cd build && cmake -G 'Unix Makefiles' -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_COMPILER=clang++ .. && cmake --build . --target compiler -- -j8"

# 3. 构建 sylib
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/build_sylib.sh"

# 4. 跑显式长度向量测试
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/run_vec_explicit_tests.sh O0"
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/run_vec_explicit_tests.sh O1"

# 5. 回归（保证不破坏已有功能）
wsl -d Ubuntu-24.04-eval -e bash -lc "cd /mnt/d/VSCodeProjects/compiler && bash scripts/run_tests.sh quick O0"
```

---

## 八、与定长版/变长版的对照

| 维度 | 定长版（`feat-simd-vector`） | 变长版（`feat-vec-dynamic-fe`） | 显式长度版（`feat-vec-explicit-len`，本分支） |
|------|--------------------------|------------------------------------|------------------------------------|
| 整数关键字 | `VEC: 'vec';` | `VEC: 'vec';` | `VEC_N: 'vec' DIGIT+;` |
| 浮点关键字 | `VECF: 'vecf';` | `VECF: 'vecf';` | `VECF_N: 'vecf' DIGIT+;` |
| 长度来源 | 初始化列表元素数 | 运行时值（`[0]` 槽） | **关键字数字部分**（编译期常量） |
| vec 表示 | `alloca [N x i32]`（N 在类型里） | `alloca [VEC_MAX+1 x i32]`（`[0]` 存长度） | `alloca [N x i32]`（N 在类型里） |
| vecf 表示 | `alloca [N x float]` | `alloca [VECF_MAX+1 x float]` | `alloca [N x float]` |
| 运算展开 | **编译期展开**：N 个标量 `load/op/store` | **运行时循环**：`alloca i` + cond/body/end 三块 | **编译期展开**：N 个标量 `load/op/store` |
| vec 识别 | `PointerType<[N x i32]>` 类型判别 | `vecAllocas` 侧集合判别 | `PointerType<[N x i32]>` 类型判别 |
| vecf 识别 | `PointerType<[N x float]>` 类型判别 | `vecfAllocas` 侧集合判别 | `PointerType<[N x float]>` 类型判别 |
| `vec a;` 默认 | 长度 4（SSE 标准宽度），零初始化 | 长度 0（空向量） | N/A（必须显式 `vecN a;`） |
| 栈占用 | `4*N` 字节 | `4*(VEC_MAX+1) = 4100` 字节 | `4*N` 字节 |
| 优化器友好度 | N 个标量指令，LoopUnroll 无作用（已展开） | 循环结构，LoopUnroll / SCCP / Mem2Reg 都能优化 | N 个标量指令，LoopUnroll 无作用（已展开） |
| 测试用例数 | 14 个（`simd_vector/01..07` 整数 + `08..14` 浮点） | 14 个（`vec_dynamic/01..07` 整数 + `08..14` 浮点） | 15 个（`vec_explicit/01..08` 整数 + `09..15` 浮点） |
| 标注 | `《前端+定长》` | `《前端+变长》` | `《前端+显式长度》` |
| 添加新长度 | 需改 IRBuilder（默认长度或初始化推断逻辑） | 自动支持（运行时长度） | **自动支持**（`VEC_N` / `VECF_N` 一条规则覆盖所有正整数） |

三版**完全互斥**（标注不同，可并存于不同分支）。最终决赛选用哪版取决于 FPGA 是否
支持 SIMD：若支持，定长版或显式长度版可后续接 RVV 后端；若不支持，变长版用运行时
循环更灵活（支持声明后改长度等动态语义）。

---

## 九、扩展空间

1. **真 SIMD 寄存器打包**：新增 `VectorType`（如 `<4 x i32>` / `<4 x float>`），TargetCodeGen 把
   连续 4 个 `load + add + store` 合并成 `vle32.v + vadd.vv + vse32.v`（需 RVV）。
   属于"后端"改动，**不在本版范围内**。
2. **运行时长度查询语法**：`len(a)` 直接读 `ArrayType::getNumElements()`（编译期常量）。
3. **更丰富的元素类型**：当前支持 `i32`（`VEC_N`）+ `float`（`VECF_N`），可扩展
   `vecd`（double）、`vecl`（i64）等（见第三节"如何添加新的元素类型"）。
4. **向量内建函数**：`dot(a, b)`、`sum(a)`、`shuffle(a, b, mask)` 等归约/重排操作。
5. **长度限制与校验**：对超大长度（如 vec1024）自动切换为运行时循环，
   避免编译期展开导致代码体积爆炸。

本版保持后端/优化器零侵入，为上述扩展保留了干净的接入点：只要在 IRBuilder 把
新语法去语法糖为标量 IR 或编译期展开 IR，无需触碰其他层。

---

## 十、文件清单

| 文件 | 状态 | 说明 |
|------|------|------|
| `grammar/SysY2022Lexer.g4` | 修改 | 加 `VEC_N: 'vec' DIGIT+;` + `VECF_N: 'vecf' DIGIT+;`（标 `《前端+显式长度》`） |
| `grammar/SysY2022Parser.g4` | 修改 | 加 `vecDecl` / `vecfDecl` / `vecInit`（标 `《前端+显式长度》`） |
| `include/ir/IRBuilder.h` | 修改 | 加 11 个方法声明（整数 5 + 浮点 6，标 `《前端+显式长度》`） |
| `src/ir/IRBuilder.cpp` | 修改 | 整数向量 9 处 + 浮点向量 7 处实现与接线（均标 `《前端+显式长度》`） |
| `src/antlr/*` | 重新生成 | 4.13.1，对齐云端 runtime，含 `VecDeclContext` / `VecfDeclContext` + `VEC_N` / `VECF_N` token |
| `test/functional/vec_explicit/01..08_*.sy` + `.out` | 新增 | 8 个整数向量测试用例 |
| `test/functional/vec_explicit/09..15_*.sy` + `.out` | 新增 | 7 个浮点向量测试用例（标 `《前端+显式长度》 浮点向量扩展`） |
| `scripts/run_vec_explicit_tests.sh` | 新增 | 显式长度向量测试运行器（覆盖整数与浮点用例） |
| `scripts/grammar/regen_antlr_4131.sh` | 新增 | ANTLR 4.13.1 重新生成脚本 |
| `显式长度向量语法_前端+显式长度_实现说明.md` | 新增 | 本文档 |

---

## 十一、定位微调点

需要微调本版实现时，用以下命令快速定位所有改动点：

```bash
# 列出所有源码改动
grep -rn '《前端+显式长度》' grammar/ include/ir/IRBuilder.h src/ir/IRBuilder.cpp

# 只看 IRBuilder.cpp 中的改动
grep -n '《前端+显式长度》' src/ir/IRBuilder.cpp

# 只看浮点向量扩展的改动
grep -rn '浮点向量扩展' grammar/ include/ir/IRBuilder.h src/ir/IRBuilder.cpp

# 统计改动点数
grep -r '《前端+显式长度》' grammar/ include/ir/IRBuilder.h src/ir/IRBuilder.cpp | wc -l
```

切换到其他版本（如"后端 + 真SIMD"或"前端 + 定长"或"前端 + 变长"）时，换一个标注名
（如 `《后端+SIMD》` 或 `《前端+定长》` 或 `《前端+变长》`）即可并存。
